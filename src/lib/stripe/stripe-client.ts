// ⚠️ DEPRECATED: Stripe client has been disabled
// Payment functionality is no longer available

export const getStripe = (connectedAccountId?: string) => {
  // DISABLED: Stripe is no longer available
  console.log("⚠️ Stripe client is disabled");
  return Promise.resolve(null);
};
